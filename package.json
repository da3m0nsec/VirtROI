{
  "name": "virtroi",
  "version": "0.1.0",
  "private": true,
  "description": "Static virtualization ROI calculator for GitHub Pages.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:js": "node --check app.js"
  }
}
